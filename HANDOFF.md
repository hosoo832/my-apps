# 인수인계 (HANDOFF) — 가계부 앱 작업 이어가기

> **새 채팅(특히 모바일 클라우드 세션)에서 이 작업을 이어받는 클로드에게.**
> 이 문서를 먼저 읽고, 사용자(한국어)와 자연스럽게 이어서 작업하세요.
> 사용자는 비개발자입니다. 쉬운 말로 설명하고, 전문 용어는 풀어서 쓰세요.

---

## 1. 지금 무슨 작업 중인가

사용자의 **개인 가계부 웹앱**을 만들고 계속 개선하는 중. 사용자의 원래 고민:
> "여기저기 매달 수입·지출이 빠져나가서 복잡하다. 한눈에 정리해서 보고 싶다."

- 앱 위치: **`budget/`** 폴더 (이 저장소 `hosoo832/my-apps` 안)
- 앱 종류: 의존성 없는 **단일 `index.html` PWA** (HTML+CSS+바닐라 JS 한 파일)
- 데이터 저장: 브라우저 **localStorage** (서버 없음, 오프라인 동작). 실제 금융 데이터는 **저장소(git)에 절대 안 올라감** — 사용자 기기 브라우저에만 저장됨.
- 웹주소(Pages 켜는 중): `https://hosoo832.github.io/my-apps/budget/`

### ⚠️ 별개 앱 주의
사용자의 **축구 앱(`gio-football`)** 은 아들이 매일 라이브로 쓰는 중이라 **건드리면 안 됨**. 그건 **다른 저장소**에 있고 여기 없음. (`CLAUDE.md`의 격리 규칙 참고)

---

## 2. 앱 현재 기능 (이미 구현됨)

`budget/index.html`에 다음이 완성되어 있음:

- **🏠 홈 대시보드**: 이번 달 수입/지출/잔액, 고정 vs 변동 막대, 카테고리별 도넛 차트, 이번 달 정기 항목 목록
- **🔁 정기 항목**: 매달 반복되는 고정 수입/지출 등록 → 매월 자동 반영, 켜고 끄기 스위치
- **📝 내역**: 변동 거래 기록(날짜별 그룹, 일별 합계), 전체/지출/수입 필터
- **⚙️ 설정**: 통계, **JSON 백업/복원(내보내기/가져오기)**, 전체 초기화
- 월 이동(‹ ›), `＋` 버튼으로 거래 추가, 모달 입력(금액/카테고리/메모/날짜)

### 데이터 구조 (import 파일 만들 때 참고)
- localStorage 키: **`budget_v1`**
- 형태: `{ "recurring": [...], "txns": [...] }`
- 정기 항목(recurring): `{ id, type:"income"|"expense", amount(숫자), category, name, day(1~31), active:true }`
- 거래(txns): `{ id, type:"income"|"expense", amount, category, date:"YYYY-MM-DD", memo, created }`
- **카테고리 id 목록**
  - 지출: `food`(식비) `cafe`(카페·간식) `mart`(생활·마트) `trans`(교통·차) `home`(주거·관리) `tel`(통신) `sub`(구독) `ins`(보험) `med`(의료·건강) `edu`(교육) `kid`(자녀) `fun`(문화·여가) `cloth`(의류·미용) `event`(경조사) `loan`(대출·이자) `save`(저축·투자) `etc`(기타)
  - 수입: `salary`(급여) `side`(부수입) `allow`(용돈) `invest`(이자·배당) `refund`(환급) `etc_in`(기타수입)
- **현재 가져오기는 "덮어쓰기"** (기존 데이터 삭제 후 교체). ↓ 아래 TODO의 "합치기" 필요.

---

## 3. 다음에 할 일 (TODO)

### (A) 고정 항목 입력 — 사용자가 불러주기로 함
사용자가 매달 고정으로 나가는 것(월세·보험·구독·통신비·대출 등)을 말해주면 → **정기 항목**으로 등록(또는 import 파일로 한 번에).

### (B) 변동 지출 입력 — 토스 캡처로 받기
사용자는 **현대카드·삼성카드·네이버페이**에서 돈이 빠져나가고, **토스 앱이 이걸 한곳에 모아줌**. 그래서:
- 사용자가 **토스 소비내역 화면 캡처(이미지)** 를 채팅에 올림 → 클로드가 읽어서 날짜·금액·카테고리로 정리 → 가계부에 넣을 데이터로 변환.
- 자동 연결(API)은 불가능(마이데이터 규제). **캡처 방식이 현실적 최선.**
- 넣기 전에 **정리된 목록을 사용자에게 보여주고 확인받기** (사용자가 토스 자동분류를 못 믿어함 → 사람 검토 필수).
- 큰 금액/애매한 건은 **물어보고** 진행.

### (C) 계좌이체 분류 규칙 — 이번 작업의 핵심 난제
사용자의 이체는 뒤죽박죽이라, **3가지로 구분**해야 함 (가계부 기준 = "돈이 가족 밖으로 나갔나?"):

| 종류 | 처리 | 예 |
|---|---|---|
| ① 내 계좌끼리 이동 | **합계에서 제외(무시/이체)** | 입출금→적금/투자 |
| ② 진짜 지출 | **지출로 기록** | 학원비, 월세 송금, 관리비 |
| ③ 와이프 정산 | 규칙 하나로 처리(아래) | 와이프가 대신 결제한 것 송금 |

**부담 줄이는 핵심 원칙:** 사용자가 매번 일일이 분류하지 않게, **"이체 상대별 규칙"을 한 번 정해두고** 그 뒤로는 클로드가 자동 분류. 처음 보는 상대만 물어봄.

**③ 와이프 정산 방식 — 사용자가 둘 중 택해야 함 (아직 미정):**
- (간단) 와이프에게 보낸 금액을 **"생활비" 지출 한 건**으로
- (자세히) 와이프가 항목 명세를 주면 **항목별로** 넣고, 송금 자체는 **무시**

> **아직 사용자에게 이체 상대 목록을 못 받음.** 이어받으면: 자주 일어나는 이체 상대(본인 다른 계좌들 / 학원·정기송금처 / 와이프)를 물어보고 규칙표를 채워나가기. 아래 "이체 규칙표"에 누적 기록.

### (D) 앱 기능 추가 (요청됨, 아직 미구현)
1. **"이체(내부이동)" 종류 추가** — 지출/수입 외 세 번째 타입. 기록은 남되 **수입/지출/잔액 합계에서 제외**. 내역에서 회색 등 중립 스타일로 표시. (①번 처리를 눈에 보이게 하려는 목적)
2. **"합치기(merge) 가져오기"** — 현재 import는 덮어쓰기라, 매달 캡처를 누적하려면 기존 데이터 유지하며 **추가만** 하는 방식 필요. 중복 방지 로직도 고려(같은 날짜+금액+메모).

---

## 4. 이체 규칙표 (작업하며 채워나갈 것)

> 사용자에게 받은 이체 상대를 여기 계속 누적하세요. 다음부터는 이 표대로 자동 분류.

| 이체 상대/메모 키워드 | 분류 | 카테고리 | 비고 |
|---|---|---|---|
| (예: 내 ○○적금) | ① 무시(이체) | - | 아직 미입력 |
| (예: △△학원) | ② 지출 | edu/kid | 아직 미입력 |
| (예: 와이프 이름) | ③ 정산 | (방식 미정) | 아직 미입력 |

---

## 5. 작업 원칙 (지킬 것)

- **단일 `index.html` 유지.** 외부 라이브러리·빌드 도구 추가 자제(오프라인·단순함 우선).
- localStorage 스키마 바꿀 땐 **기존 데이터 마이그레이션** 주의(사용자 데이터 날리지 말 것).
- 실제 금융 데이터(캡처 내용, import 파일)는 **git에 커밋 금지.** 저장소엔 앱 코드만.
- 변경 후 가능하면 미리보기로 동작 확인하고, 사용자에게 결과를 보여줄 것.
- `budget/` 폴더만 작업. 다른 앱 폴더 건드리지 말 것.

---

## 6. 사용자 환경 메모
- 한국어, 비개발자. 클로드 모바일 코드로 계속 이어서 작업하고 싶어 함.
- GitHub: `hosoo832`. 저장소: `my-apps`(가계부 등), `gio-football`(축구, 별개).
- 토스/현대카드/삼성카드/네이버페이 사용.
