# my-apps — 여러 개인 앱 모음 저장소

이 저장소는 **서로 독립적인 여러 개의 앱**을 담는 곳입니다. 각 앱은 최상위 폴더 하나로 완전히 분리되어 있습니다.

> 📌 **진행 중인 작업이 있다면 먼저 [`HANDOFF.md`](HANDOFF.md)를 읽으세요.** 이전 대화에서 어디까지 했는지, 다음에 뭘 해야 하는지 정리돼 있습니다. (현재: 가계부 앱 — 토스 캡처로 변동 지출 넣기 + 계좌이체 분류 규칙 잡는 중)

## ⚠️ 가장 중요한 규칙 (앱 간 격리)

- **한 앱을 작업할 때는 그 앱의 폴더 안에서만 파일을 수정한다.**
- 사용자가 명시적으로 지시하지 않는 한, **다른 앱의 폴더는 절대 열거나 수정하지 않는다.**
- 어떤 앱을 작업하는지 불확실하면 **추측하지 말고 사용자에게 먼저 묻는다.**
- 여기 없는 **축구 앱(`gio-football`)은 완전히 별개의 저장소**에 있고 이 저장소에 포함되지 않는다. 여기서 찾지 말 것.

## 현재 앱 목록

| 폴더 | 앱 | 설명 |
|------|----|------|
| `budget/` | 나의 가계부 | 매달 반복 수입·지출 정리 + 한눈에 보는 개인 가계부 (PWA) |
| `stock/` | 주식 종목 보고서 | 실시간 시세 + 차트 지표 + 증권사 리포트 기록을 쌓아 트래킹하는 리서치 저널 (PWA) |
| `singing/` | 노래 연습 | 악보·가사가 흐르고 마이크로 내 음정을 실시간 감지해 음·박자를 채점하는 연습 앱 (PWA) |

> 폴더 이름은 깔끔한 웹주소(GitHub Pages)를 위해 영어로 짓는다.

## 앱 공통 구조

각 앱은 의존성 없는 **단일 `index.html` PWA**입니다.

- `index.html` — 앱 본체 (HTML + CSS + 바닐라 JS 한 파일)
- `manifest.json` — PWA 설정 (홈 화면 설치용)
- `icon.svg` — 앱 아이콘
- 데이터는 브라우저 `localStorage`에 저장 (서버/백엔드 없음, 오프라인 동작)

## 새 앱을 추가할 때

1. 새 최상위 폴더를 만든다 (영어 이름, 예: `todo/`).
2. 그 안에 `index.html`, `manifest.json`, `icon.svg`를 둔다.
3. 위 "현재 앱 목록" 표와 `README.md`에 한 줄 추가한다.
4. 다른 앱 폴더는 건드리지 않는다.

## 작업 메모

- 단일 파일 유지, 외부 라이브러리/빌드 도구 추가 자제 (오프라인·단순함 우선).
- `localStorage` 스키마를 바꿀 때는 기존 데이터 마이그레이션에 주의.
